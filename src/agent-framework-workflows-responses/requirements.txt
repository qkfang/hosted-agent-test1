# Use the narrow Foundry subpackage for the chat client + workflow primitives.
agent-framework-foundry

# AG-UI protocol integration — the container serves AG-UI on /invocations for web2.
# This is a pre-release package; the Dockerfile installs with `pip install --pre`.
agent-framework-ag-ui

# Responses protocol hosting — provides ResponsesHostServer, which serves POST
# /responses (the Foundry portal Playground contract) plus /readiness and graceful
# shutdown. Pre-release; installed with `pip install --pre`.
agent-framework-foundry-hosting

# Auth to the Foundry model deployment (async credential path needs aiohttp).
azure-identity
aiohttp

# Local .env loading + the ASGI stack (fastapi/uvicorn also come via agent-framework-ag-ui).
python-dotenv
fastapi
uvicorn

mcp<2,>=1.24.0

# debugpy enables local debugging of this agent with the Foundry Toolkit VS Code extension.
debugpy
